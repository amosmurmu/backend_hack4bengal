// // prettier.config.js
// module.exports = {
//   semi: true, // semicolons at end of lines
//   singleQuote: true, // use ' instead of "
//   trailingComma: 'es5', // only where valid in ES5/ES2016 (like arrays, objects)
//   tabWidth: 2, // 2 spaces indentation
//   printWidth: 80, // wrap lines that exceed 80 chars
//   bracketSpacing: true, // { foo: bar } instead of {foo: bar}
//   arrowParens: 'avoid', // (x) => x becomes x => x
// };
